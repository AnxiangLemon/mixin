<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lemon.mixin.mapper.FriendsRequestMapper">

<select id="queryFriendsRequestVOList" resultType="com.lemon.mixin.entity.vo.FriendsRequestVO"
parameterType="string">
    select
      sender.id  as sendUserId,
      sender.username as sendUsername,
      sender.face_image as sendFaceImage,
      sender.nickname as sendNickname
    from
      friends_request fr
    left join
      users sender
    on
      fr.send_user_id = sender.id
    where
      fr.accept_user_id = #{id}
</select>


    <select id="selectfrid" resultType="com.lemon.mixin.entity.FriendsRequest" parameterType="string">
        select * from friends_request
        where accept_user_id = #{id}
    </select>
</mapper>
